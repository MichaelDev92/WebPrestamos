import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Productos",
};

export default function ProductsPage() {
  return (
    <div>
      <h1>Productos</h1>
      <p>En construcción.</p>
    </div>
  );
}
